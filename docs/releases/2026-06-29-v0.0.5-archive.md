# SuperAgentIsland v0.0.5 Release Archive

Date: 2026-06-29

## Scope

- Removed the retired second GAC account from release-time secret injection.
- Kept only the embedded it-service GAC account for GAC credits refresh.
- Centered the single account block in the GAC credits panel.
- Updated release secrets example, README, project overview, acceptance checks,
  landing page download links, and Homebrew Cask metadata for `v0.0.5`.

## Verification

- `CLANG_MODULE_CACHE_PATH=/tmp/swift-module-cache ./build.sh`
- Generated `BuildSecrets.gacAccounts` was checked to contain one account tuple
  without printing secret values.
- `git diff --check`
- `CLANG_MODULE_CACHE_PATH=/tmp/swift-module-cache ./release.sh`

## Release Assets

- `dist/SuperAgentIsland-0.0.5.dmg`
- `dist/appcast.xml`
- GitHub Release: `v0.0.5`
- DMG sha256:
  `387ddcf5650871116b20f7bf65186b796c68c24b0adaa9b742c22224dcf6e8b9`

## Notes

- Superseded by `v0.0.6`: the old-secret migration fallback in this release
  used the retired first GAC account slot instead of the active second slot.
- `GAC_IT_SERVICE_EMAIL` and `GAC_IT_SERVICE_PASSWORD` are the preferred
  release secret names.
- `GAC_ACCOUNT_1_EMAIL` and `GAC_ACCOUNT_1_PASSWORD` remain as a migration
  fallback for existing local secrets.
- `GAC_ACCOUNT_2_*` is no longer consumed by the build.
- Sparkle appcast points at
  `https://github.com/daodaolee/super-agent-island/releases/download/v0.0.5/SuperAgentIsland-0.0.5.dmg`.
