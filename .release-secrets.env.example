# Copy to .release-secrets.env for local release builds.
# Do not commit the real .release-secrets.env file.
#
# SuperAgent credentials are no longer injected at build time.
# Users configure them at runtime via Settings > Account.

GAC_ACCOUNT_1_EMAIL=""
GAC_ACCOUNT_1_PASSWORD=""

GAC_ACCOUNT_2_EMAIL=""
GAC_ACCOUNT_2_PASSWORD=""
